import * as eslint from './eslintrc';
import * as babel from './babel';
import * as prettier from './prettier';

export const SUPPORTED_CONFIGS = ['eslint', 'babel', 'prettier'];

export { eslint, babel, prettier };
